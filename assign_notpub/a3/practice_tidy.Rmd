---
title: "practice_tidy"
author: "yourname"
output: 
  html_document: 
    theme: spacelab
---

# Task
Start with the following. 

```{r hey-look}
drinks <- data.frame(
  ID = c(1, 1, 3, 4, 4),
  FirstName = c("Jean", "Jean", "Taylor", "Travis", "Travis"),
  Beverage = c("Tea", "Tea", "Coffee", "Tea", "Coffee"),
  stringsAsFactors = FALSE
)


```

# Results


# AI/Resources statement

*All assignments need about one paragraph describing the resources you used (including links and prompts, as relevant) in completing the assignment. This helps us learn about your process.*




Tidy this data frame so that it adheres to the tidy data principles:

1. Each variable must have its own column.
1. Each observation must have its own row.
1. Each value must have its own cell.

